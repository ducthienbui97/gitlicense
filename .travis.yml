language: node_js
sudo: false
node_js:
  - "node"
install:
  - npm install -g codecov
  - npm install -g istanbul
script:
  - istanbul cover ./node_modules/lab/bin/lab --report lcovonly  -- -l -m 5000  && codecov
branches:
  only:
    - master
